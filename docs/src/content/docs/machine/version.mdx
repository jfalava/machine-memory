---
title: version
description: Print the installed machine-memory CLI version.
sidebar:
  order: 2
---

## Usage

```sh
machine-memory version
```

The command prints a JSON object containing the installed semantic version. It
does not access the database and needs no backend flag.
