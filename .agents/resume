#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

export PATH="$HOME/.bun/bin:$PATH"
command -v bun >/dev/null
printf 'Orb resume check complete (Bun %s).\n' "$(bun --version)"
